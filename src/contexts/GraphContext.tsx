
import React from 'react';
import { GraphProvider, useGraph } from '../lib/graph';

// Re-export the graph context components from the new location
export { GraphProvider, useGraph };
export default useGraph;
